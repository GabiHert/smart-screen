export class CurrentParsed {
  weather: {
    main: string;
    description: string;
  };
  main: {
    temp: string;
    feels_like: string;
    temp_min: string;
    temp_max: string;
    humidity: number;
  };
}
